# PRD: 점심 메뉴 & 후식 뽑기 앱 — "오늘 뭐 먹지?"

> **Version**: 0.5 (만장일치 기반, 거부 페널티 및 단순 동기화 적용)
> **Author**: Jaesik
> **Date**: 2026-03-05
> **Status**: Draft for Review

---

## 1. 개요 (Overview)

### 1.1 배경

팀 점심 메뉴 선택은 매일 반복되는 의사결정이지만 "아무거나"라는 답이 오가며 시간이 낭비된다. 여기에 후식(커피) 비용을 누가 낼지까지 결정해야 하면 피로도가 높아진다. 이 앱은 두 가지 문제를 하나의 플로우로 해결한다.

### 1.2 제품 비전

팀이 직접 큐레이션한 식당 리스트에서 랜덤 1곳 추천 → 수락/거부 → 후식 당첨자 **복불복 뽑기**를 하나의 세션으로 묶는다. 다수결 투표가 아닌 "난 이거 먹기 싫어"라는 의견을 우선 존중하여 **단 1명이라도 거부할 시 새로운 식당을 추천**한다. 대신 메뉴를 거부한 사람에게는 **후식 뽑기 당첨 확률이 증가하는 패널티**가 주어져, 무분별한 반대를 방지하고 뽑기의 재미(조각 색상 / 개수 표시)를 극대화한다.

### 1.3 목표 사용자

소규모 팀(3~8명) — 같은 사무실/건물에서 함께 점심을 먹는 직장인 그룹.

---

## 2. 핵심 용어 정의

| 용어 | 정의 |
|---|---|
| **세션(Session)** | 한 번의 점심 이벤트 전체 사이클 (식당 선택 → 후식 뽑기) |
| **그룹(Group)** | 고정 멤버로 구성된 점심 팀 |
| **참여자(Participant)** | 해당 세션에 실제 참여하는 멤버 (그룹 멤버 중 출석자) |
| **거부(Veto)** | 메뉴 반대 의사. 1명이라도 거부 시 즉시 재추천을 트리거하며, 당사자는 후식 확률 페널티를 얻음 |
| **후식 뽑기(Dessert Draw)** | 후식 비용 부담자를 복불복으로 결정하는 행위 (룰렛) |
| **확률 모드(Draw Mode)** | 균등 / 연차·나이 차등 — 그룹 설정에서 선택 |

---

## 3. 기능 요구사항

### 3.1 그룹 & 멤버 관리

#### 3.1.1 그룹 생성

- 그룹명 설정 (예: "개발팀 점심")
- 초대 코드 생성 → 팀원이 코드 입력으로 합류
- 한 사용자가 여러 그룹에 속할 수 있음

#### 3.1.2 멤버 관리

- **고정 멤버**: 그룹에 등록된 전체 인원
- **세션 참여/불참**: 세션 시작 시 오늘 참여 여부 토글 (기본 참석)
- **점심 참여 = 후식 대상**: 점심에 참여한 사람은 자동으로 후식 뽑기 대상이 됨 (별도 후식 참여/불참 없음)

#### 3.1.3 데이터 동기화

- Firebase Firestore 기반 데이터 저장 (실시간 동기화 비권장, **앱 접속/실행 시 1회 동기화**)
- 오프라인 시 로컬 캐시 → 온라인 복귀 및 앱 재실행 시 스토어와 머지
- Firebase Auth (Google 로그인) 기반 인증

---

### 3.2 식당 리스트 관리 (큐레이션 기반)

#### 3.2.1 데이터 소스

이 앱은 실시간 주변 검색이 아닌, **팀이 사전에 큐레이션한 식당 풀(Pool)**에서 수동 등록 방식을 기본으로 운영한다.

**앱 내 수동 등록 (기본)**
- 식당명 + 주소 직접 입력
- 또는 Google Places Autocomplete로 검색 후 선택하여 쉽고 빠르게 추가
- 카테고리 태그 수동 지정 (한식, 중식, 일식, 양식, 분식, 기타)

*(참고: Google Maps '저장 장소' 일괄 임포트 기능은 API 한계 및 운영 효율성을 위해 보류)*

#### 3.2.2 식당 리스트 권한

| 행위 | 권한 |
|---|---|
| 식당 추가 | 그룹원 누구나 자유롭게 |
| 식당 삭제 | 과반수 투표 필요 (삭제 요청 → 24시간 내 투표) |
| 카테고리/메모 수정 | 그룹원 누구나 |

#### 3.2.3 식당 정보 구조

각 식당은 다음 정보를 보유한다:

- **필수**: 식당명, 주소 (또는 Google Place ID)
- **선택**: 카테고리 태그, 메모/한줄평, 평균 가격대 (직접 입력), 사진
- **자동 생성**: 등록자, 등록일, 방문 횟수, 마지막 방문일, 그룹 평균 만족도

#### 3.2.4 추천 알고리즘

1. 그룹 식당 풀에서 전체 목록 로드
2. 블랙리스트(비활성 처리된) 식당 제외
3. 최근 N일(기본 5일) 내 방문 식당 제외 (이력 기반 중복 방지)
4. 카테고리 필터 적용
5. 가중 랜덤으로 **1개 식당** 제시 (신규 식당 발견 유도 가중치 등 반영)

#### 3.2.5 수락 & 거부 플로우 (만장일치 및 패널티 기반)

```text
[1개 식당 제시]
    ↓
참여자 확인: 수락(👍) / 거부(👎) 
* 무응답(세션 타임아웃 개념 없음) 시 자동으로 '수락(찬성)'으로 표기
    ↓
├─ 단 1명이라도 거부(Veto) → 새로운 1개 식당 즉시 재추천 및 거부자 페널티(가중치) 누적
└─ 전원 수락 (무응답 포함) → 해당 식당 확정
       ↓
    (최대 3회까지 재추천 반복 가능)
       ↓
    3회 내내 거부 발생으로 매칭 실패 시 → 무작위 통과 또는 자유 입력 모드
```

> **거부 패널티:** 메뉴 거부는 팀원의 취향을 존중해 식당을 바로 바꾸지만, 그 대가로 후식 당첨 확률이 올라간다. 심리적 압박감을 줄이면서도 재미를 유발하기 위해 무기명 대신 **"결과 룰렛에서 해당 참여자의 면적(색상/개수)이 커지는 방식"**으로 직관적이고 유쾌하게 표현한다.

---

### 3.3 후식(커피) 복불복 뽑기

#### 3.3.1 참여자

- **점심 참여자 = 후식 뽑기 대상**
- 점심 세션에 참석한 모든 멤버가 자동으로 뽑기 대상

#### 3.3.2 확률 모드 및 거부 가중치

기본 확률에 **거부(Veto) 이력 가중치**가 덧붙여진다.

**모드 A: 균등 확률 기반**
- 기본 확률 동등 (예: 4명이면 각 25%)
- 단, 식당 거부 시마다 페널티로 개인 영역 가중치 증가

**모드 B: 연차/나이 기반 차등 확률 기반**
- 연차나 나이에 기반해 그룹이 지정한 가중치(Weights)
- 이 베이스라인 위에 메뉴 거부 건당 추가 페널티 가중치가 반영됨

#### 3.3.3 뽑기 UX (가장 중요)

- 룰렛(슬롯 보다는 룰렛 형태 권장) 스타일 애니메이션
- 각 참여자의 당첨 확률을 **룰렛 조각의 개수 또는 색상의 면적**으로 명시적으로 시각화 (예: 거부를 많이 한 사람은 자기 이름의 крас(빨강) 영역이 큼)
- 결과 화면: 당첨자 강조 + 전체 확률 공개
- "다시 뽑기" 없음 (단호하게)

---

### 3.4 식당 상태 관리

#### 3.4.1 식당 상태

| 상태 | 의미 | 추천 대상 |
|---|---|---|
| **활성(Active)** | 정상 추천 대상 (기본) | O |
| **즐겨찾기(Favorite)** | 추천 가중치 상승 | O (가중치 ×1.5) |
| **비활성(Inactive)** | 추천에서 제외 | X |

#### 3.4.2 상태 변경 권한

- Active ↔ Favorite: 언제나 토글 가능
- Active ↔ Inactive: 삭제 과정과 유사한 과반 투표 프로세스

---

### 3.5 이력 관리

#### 3.5.1 기록 항목

- 날짜, 선택된 식당, 참여 멤버
- 추천 → 수락/거부(1인 거부 시) 이력 (몇 라운드 진행)
- 후식 당첨자, 실 적용된 개별 확률 리스트(페널티 합산 분)

#### 3.5.2 통계 대시보드

- 이번 달 가장 많이 간 식당 Top 5
- 누적 메뉴 거부 횟수 ("가장 입이 까다로운 호불호왕")
- 후식 당첨 횟수 ("우리 팀 커피 요정")

---

## 4. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| **플랫폼** | Android 우선 (Flutter), iOS 후속 |
| **최소 지원 버전** | Android 8.0 (API 26) |
| **인증** | Firebase Auth (Google 로그인) |
| **데이터베이스** | Cloud Firestore (앱 실행 시 1회 동기화/Fetch 방식) |
| **지도 연동** | Google Maps Platform (Places Autocomplete) |
| **오프라인** | Firestore 로컬 캐시 사용, 네트워크 재연결 시 동기화 |
| **타임아웃** | 투표/세션 타임아웃 없음(무응답 자동 찬성) |
| **알림** | MVP에서는 앱 외 메신저 공유로 유도(FCM 배제) |

---

## 5. 기술 아키텍처

### 5.1 전체 구조 (요약)

Flutter (UI/State) ↔ Repository ↔ Firebase (Auth, Firestore 1회 동기화) + Google Places API 연계

### 5.2 Firestore 모델링 시 고려사항 (주요 변경점)

```
/groups/{groupId}
  - settings:
    - recentExcludeDays: number (기본 5)
    - maxVetoRounds: number (기본 3)
    - drawMode: enum (equal | weighted)
    - vetoPenaltyWeight: number (거부 시 추가되는 룰렛 확률 가중치 양)

/sessions/{sessionId}
  - vetoRecords: { uid: count } (이번 세션에서 패널티 받은 누적 거부 횟수록 보관)
  - selections: ...
```

---

## 6. 핵심 유저 플로우

### 6.1 일반 플로우 (Happy Path)

1. 슬랙으로 세션 시작 안내 (수동 공유 가능)
2. 팀원들이 앱 접속 (앱 킬 때 1회 데이터 최신화 동기화)
3. 1곳 자동 추천 → 수락 누르거나 앱 끄고 가만히 냅둠 (자동 수락)
4. 만장일치(무응답 포함) 자동 수락 시 식당 확정. 이동하여 식사
5. 점심 식사 후 → "후식 뽑기" 앱 실행
6. 점심 참여자 전원 확률 룰렛 (패널티가 없으면 균일/기본 차등 색상 배분) → 당첨자 결제

### 6.2 거부(Veto) 플로우

1. "불뼈찜" 추천 → 매운걸 못 먹는 A가 **"거부(👎)"** 터치
2. 1명이라도 반대 시: 즉시 식당 폐기, A의 룰렛 당첨 확률(=지분) 누적 증가
3. 시스템 바로 재추천 (예: "수제버거") → 모두 냅둠(수락)
4. 점심 후 룰렛 실행: **룰렛상에 A의 구역(조각/색상)이 직관적으로 확 늘어나 있음**. 투명하게 돌리고 당첨자 결제

---

## 7. 부록 A: 확정된 설계 결정 요약

| # | 항목 | 결정 내용 |
|---|---|---|
| 1 | 식당 제시 방식 | **1개 제시 후 만장일치 여부 판단** (다수결 배제) |
| 2 | 거부 결정/페널티 | 단 1명이라도 거부하면 재추천하나, **해당 멤버의 후식 룰렛 영역이 늘어남** (심리적 부담감은 유쾌한 룰렛 UI로 승화) |
| 3 | 무응답 처리 | 귀찮음을 방지하기 위해 타임아웃 없이 **무응답은 무조건 수락(찬성) 간주** |
| 4 | 동기화 타이밍 | 과도한 실시간 리스너 대신 **앱 진입 시점 능동 업데이트(Fetch)** 방식 사용 |
| 5 | 지도/장소 연동 | 개인 Saved Place 임포트 제외, **직접 검색(Autocomplete) 수동 등록**만 지원 |
| 6 | 푸시 알림 | MVP 단계에서 FCM 미사용, 추후 Phase 3 스펙으로 보류 |
| 7 | 후식 확률 모드 | 균등/차등 설정 존재하나 거부(Veto) 페널티 가중치는 공통 합산 |